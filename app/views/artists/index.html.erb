<div class="container">
  <div class="row">
    <div class= "col-sm-12">
      <%= render 'navbar' %>
    </div>
  </div>

  <div class="container">
    <div class="row">
      <% @artists.each do |artist| %>
        <div class="col-sm-3 pt-3">
          <div class="card">
            <img class="card-img-top p-1" src="<%= artist.image_url %>" alt="artistas">
            <div class="card-body">
              <h5 class="card-title text-center"><strong><%= artist.name %></strong></h5>
              <%= link_to "Ver más", artist_path(artist.id), class:"btn btn-primary btn-block"%>
            </div>
          </div>
        </div>
      <% end %>  
    </div>  
  </div>
</div>
